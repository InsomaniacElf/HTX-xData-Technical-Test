services:
  asr-api:
    build:
      context: .
      dockerfile: asr/Dockerfile
    ports:
      - "8001:8001"
    gpus: all
    environment:
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
